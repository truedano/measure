# Photo Measurement Tool

一個強大且隱私安全的相片尺寸測量工具，支援透過自訂參考基準尺規或 DPI 數值來精確測量相片中的任意線段與實體尺寸。

👉 **線上展示 / Demo**: [https://measure-azure.vercel.app/](https://measure-azure.vercel.app/)

---

## 核心功能 (Core Features)

- 📏 **自訂參考基準尺規 (Reference Calibration):** 可自由設定一條已知實體長度的參考線段（例如悠遊卡、硬幣或尺規），工具將以此為基準自動推算其他線段的實體尺寸。
- 🎯 **精確量測線段 (Precision Measurements):** 支援以像素 (px) 或自訂實體單位（如 mm、cm、inch 等）來繪製並計算多組測量線段。
- 🌀 **圖片任意角度旋轉微調 (Arbitrary Image Rotation):** 支援 0° ~ 360° 的任意角度旋轉校正，在平移與縮放時可保持完美的對齊與渲染，輕鬆搞定傾斜的相片。
- 🎨 **線段懸停高亮與多色彩區分 (Line Hover Highlighting & Color Coding):**
  - 滑鼠懸停於測量線段時，畫布與右側清單中的對應項目會同步加粗高亮。
  - 不同線段自動以不同的高對比顏色繪製，方便在複雜圖面中清晰辨識。
- 📊 **可縮放數據表格 (Resizable Data Sheet):** 數據表格面板頂部新增拖曳條，可自由拖曳調整表格高度，並將高度喜好持久化儲存於本地 `localStorage`。
- ⌨️ **快捷鍵快速刪除 (Keyboard Shortcut Deletion):** 支援滑鼠懸停於線段上時，直接按下 `Delete` 或 `Backspace` 快速刪除該測量線，且內建輸入框聚焦保護，避免輸入備註時誤刪。
- 🔗 **表格高亮外框圈選 (Data Sheet Outlining):** 當滑鼠移動到畫布線段或右側測量清單時，底部數據表中代表該線段的數據列會即時同步高亮，並以霓虹藍色外框明顯圈選。
- 🏷️ **懸停清單顯示備註 (Note Displays in Hover List):** 右側浮動清單除顯示線段編號與長度外，亦會同步顯示該線段之自訂備註，並支援長文字省略與 Hover 完整提示。
- 🧭 **繪圖輔助與正交鎖定 (Drawing Assist & Angle Snapping):**
  - **氣泡提示**：繪圖時滑鼠游標旁顯示動態操作指示（起點/終點提示），並提示功能引導。
  - **正交鎖定**：按住 `Shift` 鍵可將繪製或拖曳控制點時的夾角自動限制在 0°/45°/90° 正交角。
  - **ESC 鍵取消**：按 `Escape` 鍵可隨時退出繪圖狀態並自動清理未完成線段。
- 💾 **IndexedDB 狀態持久化 (IndexedDB Workspace Auto-Save):** 自動在本地 IndexedDB 儲存目前的工作狀態。即使重新整理網頁或關閉瀏覽器，再次開啟時亦能完整還原您的所有圖片、測量線段、旋轉角度及最後瀏覽的圖片狀態。
- 🖼️ **工作區多圖管理 (Workspace Multi-Image Management):**
  - 支援同時上傳並切換多張圖片。
  - 圖片管理區新增 **一鍵清除所有圖片 (Clear All)** 的快速清理功能。
- 🏷️ **版本號動態顯示 (Dynamic Version Display):** 工具列標題右側動態顯示當前 `package.json` 設定之版本號。
- 🔒 **本地離線處理 (Local & Offline First):** 所有圖片及測量數據均完全在您的瀏覽器本地處理，絕不上傳至任何伺服器，確保隱私安全。

---

## 使用說明 (How to Use)

1. **載入圖片 (Load Image):** 點擊上傳或拖放圖片至工作區。
2. **設定參考基準 (Set Reference Size):** 點擊「Add Reference Size」，在畫布上繪製一條已知長度的線段，並輸入其長度與單位。
3. **輸入 DPI (Input DPI):** 若圖片為已知 DPI 的掃描檔，可直接輸入 DPI 值來自動計算比例。
4. **新增測量線段 (Add Measurement Lines):** 點擊「New Measurement Line」，即可在圖片上繪製任意想測量的長度。
5. **微調與刪除 (Adjust & Remove):** 拖曳線段端點即可微調，或點擊「✖」刪除該線段。
6. **校正圖片角度 (Rotate Image):** 利用工具列的旋轉滑桿或輸入數值，可對圖片進行任意角度的旋轉校正。

---

### ⚠️ 重要注意事項 (Important Perspective Notice)

**透視變形與測量誤差：**
如果您使用的是普通相機拍攝的相片，請注意**透視變形（Perspective Distortion）**對測量精度的影響。
只有當被測物體與參考尺規處於**同一水平面**，且**垂直於相機鏡頭的拍攝軸線**時，測量結果才具備高精確度。
請避免使用傾斜、有深度差或斜角平面上的參考基準，否則會因為近大遠小的透視效應導致嚴重的計算偏差。

---

## 技術堆疊 (Tech Stack)
- Vue 3 (Composition API)
- TypeScript
- Vite
- Pinia (狀態管理)
- IndexedDB (本地持久化)

---

## 更新日誌 (Changelog)

### v0.3.2 (2026-07-16)
- ✨ **新增**: 支援在繪圖或拖曳控制點時按住 `Shift` 鍵鎖定夾角為正交角度（0°/45°/90°）。
- ✨ **新增**: 滑鼠游標旁新增動態引導氣泡提示（Cursor Tooltips），即時顯示下一步引導，提升易用性。
- ✨ **新增**: 支援按鍵盤 `Escape` 鍵隨時中斷並退出當前繪圖模式，且自動清理半完成線段。
- 📝 **文件**: 將正交鎖定與氣泡引導工具規格正式記錄於專案規格書 `PRD.md` 內。

### v0.3.1 (2026-07-16)
- 🐛 **修復**: 解決畫布上測量線與參考線中心點的 `x` 刪除按鈕因滑鼠事件穿透（pointer-events）與碰撞偵測衝突，導致點擊無效且閃爍消失的 Bug。

### v0.3.0 (2026-07-16)
- 📊 **新增**: 數據表格面板（DataSheet）支援高度自由拖曳調整（Resize），並將調整後的高度偏好持久化儲存於 `localStorage`。
- ⌨️ **新增**: 支援滑鼠懸停於測量線段上時直接按 `Delete` 或 `Backspace` 快速刪除線條，且防禦文字輸入框避免誤刪。
- 🔗 **新增**: 支援滑鼠懸停於頂部 UI 清單或畫布線段時，底部數據表對應數據列會同步高亮並以實心霓虹藍框圈選。
- 🏷️ **新增**: 於右側浮動面板中同步呈現線段的自訂「註解/標籤」，且支援長文字省略與 Hover 完整提示。
- 🐛 **修復**: 修正底部備註輸入時沒有自動觸發 IndexedDB 資料同步，造成重整頁面後修改消失的 Bug。

### v0.2.1 (2026-07-14)
- 🐛 **修復**: 移除了 `CanvasArea.vue` 中未使用的 `getLineStyle` 函式，修正 Vercel 部署時因 TypeScript 嚴格檢查（`noUnusedLocals`）造成的編譯建置失敗。

### v0.2.0 (2026-07-14)
- ✨ **新增**: 支援圖片 0° ~ 360° 任意角度旋轉校正，改善旋轉狀態下的縮放與平移對齊渲染。
- 🎨 **新增**: 測量線段滑鼠懸停加粗高亮連動，且各線段自動採用不同高對比色彩標記，大幅提升複雜圖面辨識度。
- 💾 **新增**: IndexedDB 工作區自動儲存與狀態還原，防止頁面重整後進度遺失。
- 🖼️ **新增**: 圖片區域支援一鍵清除所有工作區圖片 (Clear All) 的快速清理功能。
- 🏷️ **新增**: 於工具列標題右側動態讀取並呈現應用程式版本號標籤。
- 🔧 **優化**: 優化與修復了 IndexedDB 資料持久化與最後選取圖片的還原邏輯。


